# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# env
.env
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor
.vscode/*
!.vscode/extensions.json
.idea

# Worker de Go: artefacto de build. Render lo compila desde el Dockerfile
# (COPY . . + go build), asi que el binario nunca se despliega desde el repo.
services/whatsapp/hiraticket-whatsapp

# Cache incremental de TypeScript ("incremental": true en tsconfig.json).
# Es por maquina y se regenera solo; compartirlo por git solo genera ruido.
tsconfig.tsbuildinfo
