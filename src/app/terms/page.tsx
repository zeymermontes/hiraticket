import { TermsContent } from "@/components/legal/TermsContent";

export const metadata = {
  title: "Términos de Servicio — Hiraticket",
  description: "Los términos que rigen el uso de Hiraticket.",
};

export default function TermsPage() {
  return <TermsContent />;
}
